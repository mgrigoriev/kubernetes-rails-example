# Run to select namespace (applying this config):
#   kubectl config set-context --current --namespace=rails-app-dev
{
  "apiVersion": "v1",
  "kind": "Namespace",
  "metadata": {
    "name": "rails-app-dev",
    "labels": {
      "name": "rails-app-dev"
    }
  }
}
